const Comment = {
    // author(parent, args, { db }, info) {
    //     return db.users.find((user) => {
    //         return user.id === parent.author
    //     })
    // },
    // recipe(parent, args, { db }, info) {
    //     return db.recipes.find((recipe) => {
    //         return recipe.id === parent.recipe
    //     })
    // }
}

export { Comment as default }